# Task 2.3: Accessibility Audit Report

## 1. Document Structure & Checklist Compliance
* **Language Tags:** Configured `<html lang="en">` explicitly on the home layout so screen readers recognize the text flow correctly.
* 
* **Image Assets:** Verified that the profile image tag has a matching descriptive `alt="mercy muiruri profile picture"` attribute string.
* 
* **Heading Hierarchy:** Enforced clean structural hierarchy using sequential heading elements (`<h1>` and `<h2>`).

## 2. Final Lighthouse Verification Score
* **Accessibility Rating:** 100 / 100
